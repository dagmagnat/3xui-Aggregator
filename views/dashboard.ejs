<%- include('partials_header', { pageTitle: 'Главная', pageSubtitle: 'Обзор узлов, клиентов и общей подписки', activePage: 'dashboard' }) %>

<div class="grid">
  <section class="stats-card span-3">
    <div class="stats-label">Узлов</div>
    <div class="stats-number"><%= stats.nodes %></div>
  </section>
  <section class="stats-card span-3">
    <div class="stats-label">Клиентов</div>
    <div class="stats-number"><%= stats.clients %></div>
  </section>
  <section class="stats-card span-3">
    <div class="stats-label">Онлайн</div>
    <div class="stats-number"><%= stats.online %></div>
  </section>
  <section class="stats-card span-3">
    <div class="stats-label">Оффлайн</div>
    <div class="stats-number"><%= stats.offline %></div>
  </section>

  <section class="card span-12">
    <h2>Как это работает</h2>
    <ol class="list-steps">
      <li>Добавляешь сюда свои 3x-ui узлы.</li>
      <li>Для каждого узла указываешь inbound ID, куда надо добавлять клиентов.</li>
      <li>Создаёшь клиента на основном 3x-ui сервере, затем импортируешь его сюда.</li>
      <li>Общая подписка доступна по ссылке вида <span class="code-inline"><%= baseUrl %>/sub/SLUG</span>, где <span class="code-inline">baseUrl</span> берётся из переменной окружения <span class="code-inline">BASE_URL</span>.</li>
    </ol>
  </section>

  <section class="card span-6">
    <h2>Последние узлы</h2>
    <div class="table-wrap">
      <table>
        <thead>
          <tr><th>Имя</th><th>Статус</th></tr>
        </thead>
        <tbody>
          <% nodes.forEach(node => { %>
            <tr>
              <td><%= node.name %></td>
              <td>
                <span class="badge <%= node.last_status || 'unknown' %>"><%= node.last_status || 'unknown' %></span>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </section>

  <section class="card span-6">
    <h2>Последние клиенты</h2>
    <div class="table-wrap">
      <table>
        <thead>
          <tr><th>Логин</th><th>Подписка</th></tr>
        </thead>
        <tbody>
          <% clients.forEach(client => { %>
            <tr>
              <td><a href="/clients/<%= client.id %>"><%= client.display_name %></a></td>
              <td><a href="<%= baseUrl %>/sub/<%= client.sub_slug %>" target="_blank">Открыть</a></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </section>
</div>

<%- include('partials_footer') %>
